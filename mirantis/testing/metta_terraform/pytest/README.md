# Mirantis Metta terraform test

Use a known terraform chart to test that metta can use the terraform provisioner to bring up a
cluster.

## Requirements

The current chart uses AWS, so you will need active AWS credentials in place.
