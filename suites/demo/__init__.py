"""

Demo MTT Package

This __init__ is empty but needed so that conftest can include the current
path as a package namespace, which will allow it to include the .uctt module.

That module defines the environment for both pytest and conftest.

"""
