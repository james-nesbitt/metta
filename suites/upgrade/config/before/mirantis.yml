# Environment: BEFORE mirantis config
presets:

    # release preset is set per environment, so we ignore it heres
    # this will tell metta to include metta/config/release/release/2020Q4
    release: release/2020Q4
